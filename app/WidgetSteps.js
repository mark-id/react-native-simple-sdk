/**
 * MARKID REQUIRED WIDGET STEPS
 */
export const widgetSteps = {
    'FACE': 1,
    'DOCTYPE': 2,
    'DOC': 3,
    'DOCBACK': 4,
    'STATUS': 5,
}

/**
 * MARKID IDENTIFICATION STATUS MESSAGES
 */
export const IdentificationStatus = {
    SUCCESS: 'success',
    WAITING_AGENT: 'waiting agent',
    INITIALIZATION: 'initialization',
    REJECTED: 'rejected'
}

/**
 * MARKID SUPPORTED DOCUMENT TYPES
 */
export const MarkIDSupportedDocumentTypes = {
    ID: 'id',
    PASSPORT: 'passport',
    PERMIT: 'permit'
}

export const TakenPictureText = 'Jūsų nuotrauka užfiksuota, eikite toliau arba pakartokite nuotrauką';